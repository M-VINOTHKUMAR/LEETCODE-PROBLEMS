class Solution {
    public boolean isPowerOfFour(int n) {
       long a=1;
        while(n>=a)
        {
            if(n==a)
                return true;
            a=a*4;
           // System.out.println(a);
        }
        return false;
    }
}
