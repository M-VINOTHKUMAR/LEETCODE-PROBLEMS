class Solution {
    public double myPow(double x, int n) {
      /*  double sum=1;
        if(n>0)
        {
        for(int i=0;i<n;i++)
        {
            sum*=x;
        }
        }
        else if(n<0)
        {
            x=1/x;
          for(int i=0;i<n;i++)
        {
            sum*=x;
        }
        }*/
        double v=Math.pow(x,n);
        return v;
    }
}
