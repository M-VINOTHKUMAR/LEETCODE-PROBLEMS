class Solution {
    public int arraySign(int[] nums) {
    //  long  p=1;
      int c=0;
        for(int i=0;i<nums.length;i++)
        {
           if(nums[i]==0) return 0;
           if(nums[i]<0) c++;
        }
        //System.out.println(p);
        return (c%2==0)?1:-1;
    }
}
