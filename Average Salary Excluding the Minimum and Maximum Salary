class Solution {
    public double average(int[] salary) {
        Arrays.sort(salary);
        int d=0,ds=0;
        for(int i=1;i<salary.length-1;i++)
        {
            d+=salary[i];
            ds++;
        }
         return (double) d/ds;
    }
}
