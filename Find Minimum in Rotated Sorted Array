class Solution {
    public int findMin(int[] nums) {
        int s=0,e=nums.length-1;
         int target=Integer.MAX_VALUE;
   for(int i=0;i<e+1;i++)
   {
    if(target>nums[i])
        target=nums[i];
   }
return target;
    }
}
