class Solution {
    public String defangIPaddr(String address) {
        String t="";
         String c="[.]";
        for(int i=0;i<address.length();i++)
        {
            if(address.charAt(i)=='.')
               t+=c;
            else
               t+=address.charAt(i);
        }
        return t;
    }
}
