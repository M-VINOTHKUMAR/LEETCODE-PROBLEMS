class Solution {
    public int returnToBoundaryCount(int[] nums) {
        int a=0,b=0;
        for(int i=0;i<nums.length;i++)
        {
          
              a+=nums[i];
              if(a==0)
                b++;
        }
        return b;
    }
}
