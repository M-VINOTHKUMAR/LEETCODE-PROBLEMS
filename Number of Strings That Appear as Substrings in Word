class Solution {
    public int numOfStrings(String[] patterns, String word) {
       int s=0;
       for(String k: patterns)
       {
        if(word.contains(k)) s++;
       }
        return s;
    }
}
